import type { StoryBeat } from '../../types/story';
import { EINSTEIN_BEATS } from './einstein';

/** Every short beat in the game, in no particular order. */
export const BEATS: StoryBeat[] = [...EINSTEIN_BEATS];
